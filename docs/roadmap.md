# Roadmap

1. Finish moving any remaining feature-specific content reads to the backend API.
2. Add pagination and category endpoints where content grows large.
3. Replace JSON repositories with PostgreSQL repositories without changing the frontend contract.
4. Add authentication and admin tooling after the content/API split is stable.
